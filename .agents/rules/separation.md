---
trigger: always_on
---

The project should have a clear separation of levels, as
controllers,
db,
views (frontend validators shall be included),
config,
constants,
models,
routes,
services,
main app(app.js)
